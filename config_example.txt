# Конфигурационный файл


# Запуск автотестов: перейти в директорию '../runner', набрать 'python main.py'

# Конфигурационные файлы 'config_..' и фалы сценариев прогонов 'scen_..' должны лежать в одной папке с папкой 'runner'
# пример: +-- hand_runner
#         |
#         +---+-- config_...txt
#             |
#             +-- scen_...txt
#             |
#             +-- runner
#             |
#             +---+-- main.py


# Значения в данном конфигурационном файле заполняются в строках не начинающихся с символа '#' и после знака ':'
# Имена конфигурационных файлов и файлов сценариев прогона должны начинаться с config_ и scen_



# Режим обновления эталонов 
# 'cpcf' - копировать файл конфигурации .cf из папки указанной в build_files 
# 'uprep' - взять .cf из хранилища конфигурации
# 'ret' - не обновлять, эталоны уже обновлены ранее
# 'uprep+ret' - не скачивать эталоны и другие данные, только обновится из репозитория
# (пример PREPARE_MODE = cpcf)

PREPARE_MODE = 


#  Каталог на компьютере(в нашем примере "sr") из которого копируются все данные на машину где будет проходить тест.
#  в каталоге должны быть тесты, файл cf конфигурации, файлы dt баз менеджера и клиента тестирования, каталоги с Ванессой.
#  Все вышеперечисленные файлы должны быть расположенный в подкаталогах следующим образом:
#  1. пример пути до каталога BUILD_FILES = \\chupakabara.do3\Users\chupakabara\BuildAddFiles\Retail\sr
#  2. карта подкаталогов и их наполнения:
#    +-- sr (каталог)
#    |
#    +---+-- cf (подкаталог с файлом .cf)
#        |
#        +---+-- 1Cv8.cf (файл конфигурации на которой будут проводиться тесты)
#        |
#        +-- model (подкаталог с эталонами .dt)
#        |
#        +---+-- manager.dt (файл менеджера тестирования, должен иметь имя "manager.dt")
#        |   |
#        |   +-- client_1.dt (эталоны баз для тестирования)
#        |   |
#        |   +-- client_2.dt
#        |
#        +-- platforms (подкаталог с платформой)
#        |
#        +---+-- 8.3.15.1656 (папка с платформой, такое же название должно быть у настройки "PLATFORM_NUMBER")
#        |
#        +-- tests (подкаталог с тестами)
#        |
#        +---+-- test.txt (текстовый файл с выгруженным после записи тестом, может быть много)
#        |   |
#        |   +-- head.txt (сценарий который должен прикрепляться к началу или концу основных тестов, может быть много, не обязателен)
#        |
#        +-- vanessa (подкаталог с Ванессой)
#        |
#        +---+-- vanessa-automation (папка)
#            |
#            +-- vanessa-tools (папка)

BUILD_FILES = 


# Версия платформы (пример PLATFORM_NUMBER = 8.3.15.1656)

PLATFORM_NUMBER = 


# Каталог c платформами 1С на компьютере где будет проводиться тестирование 
# (пример PLATFORM_PATH = C:\Program Files (x86)\1cv8\)

PLATFORM_PATH = 


# Каталог с файлом "1cestart.exe" на компьютере где будет проводиться тестирование 
# (пример STARTER_1C = C:\Program Files (x86)\1cv8\common\1cestart.exe)

STARTER_1C = 


# Данные о компьютере с которого будет копироваться папка с тестовыми файлами (в примере папка "sr") 
# (пример NET_DOMAIN = do3 , NET_USER = chupakabara , NET_PASSWORD = qwerty)

NET_DOMAIN = 
NET_USER =
NET_PASSWORD =


# Данные о хранилище 1С конфигурации 
# (пример REPO_PATH = tcp://jan.do3:4343/Storage, REPO_VER:34)

REPO_PATH = 
REPO_VER =
REPO_USER = 
REPO_PASSWORD =


# Если автотесты нужно брать из git репозитория 
# (пример  GIT_REPO_TESTS = https://jan.do3/chupakabara/tests.git)

GIT_REPO_TESTS =

# Каталог для отчетов (пример REPORTS = C:\)

REPORTS = 
